import React from "react";
import "./FlashDeals.style.scss";

const FlashDealsMobilePresenter: React.FC = (props) => {
  return <div>FlashDeals mobile</div>;
};

export default FlashDealsMobilePresenter;
